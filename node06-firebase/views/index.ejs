<%- include('partials/header', {title: 'Bootstrap Template'} ); %>
  <div class="contaner-fluid">
    <div class="row">
      <% for( var i=0; i < categories.length ; i++){%>
      <div class="col-sm-12">
        <div class="thumbnail">
            <img src="/images/<%= categories[i].thumbnail; %>" alt="<%=categories[i].title%>">
            <div class="caption">
                <h3><%=categories[i].title; %></h3>
                <p><%- categories[i].description; %></p>
                <p><a href="#" class="btn btn-primary" role="button">View Items</a></p>
            </div>
         </div>
        </div>
      <% } %>
    </div>
  </div>
<%- include('partials/footer'); %>
